 #include "main.h"

/**
 *  * _abs - Entry point
 *   * @n: carrier variable
 *    * Description: compute the absolute value of an integer
 *     * Return: 0 OR 1
 */

int _abs(int n)
{
if (n > 0)
{
return (n);
}
else if (n == 0)
{
return (n);
}
else
{
return (-n);
}
}
